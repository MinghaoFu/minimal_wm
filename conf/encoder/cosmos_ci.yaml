# @package _global_

original_patches: 784
compressed_patches: 196

encoder:
  _target_: models.cosmos_ci.CosmosCIEncoder
  model_id: "nvidia/Cosmos-0.1-Tokenizer-CI8x8"
  # Use the input size as-is (no internal resize) to keep tokenizer behavior intact.
  image_size: null
  torch_dtype: "float16"
  emb_dim: 16
